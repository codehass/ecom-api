<!DOCTYPE html>
<html>
  <head>
    <title>Ecom</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class='container mx-auto mt-28 px-5 flex'>
      <nav class='flex z-full'>
        <div class='z-64 p-4 h-screen bg-gray-600 aticky top-0'>
          <div class='flex'>
            <% image_tag 'logo.png', class: 'z-16' %>
            <h1 class='text-md py-2 text-gray-50 font-bold hidden md:block'>Ecom Admin</h1>
          </div>
          <ul class='flex flex-col text-gray-50'>
            <li>
              <%= link_to admin_path, class: 'block  px-4 py-2 hover:rounded hover:bg-gray-700' %>
              <% icon('fa-solid', 'gauge-high') %><span class='hidden md:inline'>Dashboard</span> 
              
            </li>
          </ul>
        </div>
      </nav>
    </main>
    <%= yield %>
  </body>
</html>
